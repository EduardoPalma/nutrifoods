import { Navbar, Container, Nav, NavDropdown, Form, FormControl, Button } from "react-bootstrap"

//otra forma de decir funcion
export const Counter = () => {
    return (
        <div>

        </div>
    )
}